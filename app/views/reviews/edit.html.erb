<h2>Edit Your Review</h2>
<div class='review-form'>
  <%= form_for @review, url: review_path(@review), method: :patch do |f| %>
    <fieldset>
      <%= f.label :rating %>
      <%= f.select(:rating, options_for_select([["🌟", 1],["🌟🌟", 2],["🌟🌟🌟", 3], ["🌟🌟🌟🌟", 4]])) %>
    </fieldset>
    <fieldset>
      <%= f.label :text %>
      <%= f.text_area :text, html: { value: @review.text } %>
    </fieldset>
    <%= f.hidden_field :user_id, html: { value: @review.user.id } %>
    <%= f.hidden_field :movie_id, html: { value: @review.movie.id } %>
    <%= f.submit %>
  <% end %>
</div>
